# LLM provider — set one. AGENT_MODEL overrides the per-provider default.
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# AGENT_MODEL=claude-haiku-4-5

# Database — omit for local dev (falls back to file:local.db).
# Production: point at a Turso/libSQL instance.
# TURSO_DATABASE_URL=
# TURSO_AUTH_TOKEN=
