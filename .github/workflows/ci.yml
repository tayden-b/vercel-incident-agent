name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      # Prisma client types are imported across the app; typecheck needs them.
      - run: npx prisma generate
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
